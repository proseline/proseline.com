{
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "index": {
      "type": "integer",
      "minimum": 0
    },
    "payload": {
      "type": "object"
    },
    "publicKey": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "signature": {
      "type": "string",
      "pattern": "^[a-f0-9]{128}$"
    }
  },
  "required": [
    "project",
    "index",
    "payload",
    "publicKey",
    "signature"
  ],
  "additionalProperties": false
}
