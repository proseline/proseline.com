{
  "type": "object",
  "properties": {
    "message": {
      "type": "object",
      "properties": {
        "project": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        "index": {
          "type": "integer",
          "minimum": 0
        },
        "body": {
          "type": "object",
          "properties": {
            "type": {
              "type": "string",
              "enum": [
                "draft",
                "intro",
                "mark",
                "note"
              ]
            }
          },
          "required": [
            "type"
          ],
          "additionalProperties": true
        }
      },
      "required": [
        "project",
        "index",
        "body"
      ],
      "additionalProperties": false
    },
    "publicKey": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "signature": {
      "type": "string",
      "pattern": "^[a-f0-9]{128}$"
    }
  },
  "required": [
    "message",
    "publicKey",
    "signature"
  ],
  "additionalProperties": false
}
