{
  "type": "object",
  "properties": {
    "entry": {
      "type": "object",
      "properties": {
        "project": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        "index": {
          "type": "integer",
          "minimum": 0
        },
        "payload": {
          "type": "object"
        }
      },
      "required": [
        "project",
        "index",
        "payload"
      ],
      "additionalProperties": false
    },
    "publicKey": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "signature": {
      "type": "string",
      "pattern": "^[a-f0-9]{128}$"
    }
  },
  "required": [
    "entry",
    "publicKey",
    "signature"
  ],
  "additionalProperties": false
}
